import SwiftUI

extension Color {
    static let cardWhite = Color.white
    static let backgroundGray = Color(UIColor.systemGray6)
    static let primaryBlue = Color.blue
    static let secondaryMint = Color.green  // Dummy-Farbe, passe nach Bedarf an
}